/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import ExperienceCard from './ExperienceCard';

storiesOf('ExperienceCard', module).add('default', () => <ExperienceCard />);
