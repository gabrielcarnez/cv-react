/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import SkillsList from './SkillsList';

storiesOf('SkillsList', module).add('default', () => <SkillsList />);
